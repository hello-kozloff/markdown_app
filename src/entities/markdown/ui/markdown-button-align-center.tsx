import { AlignCenter } from "lucide-react";
import { MarkdownButton } from "./markdown-button";

export function MarkdownButtonAlignCenter() {
  return (
    <MarkdownButton>
      <AlignCenter size={16} />
    </MarkdownButton>
  );
}
