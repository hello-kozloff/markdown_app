import { ImageIcon } from "lucide-react";
import { MarkdownButton } from "./markdown-button";

export function MarkdownButtonImage() {
  return (
    <MarkdownButton>
      <ImageIcon size={16} />
    </MarkdownButton>
  );
}
