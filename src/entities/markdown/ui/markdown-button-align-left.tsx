import { AlignLeft } from "lucide-react";
import { MarkdownButton } from "./markdown-button";

export function MarkdownButtonAlignLeft() {
  return (
    <MarkdownButton>
      <AlignLeft size={16} />
    </MarkdownButton>
  );
}
