import { CodeXml } from "lucide-react";
import { MarkdownButton } from "./markdown-button";

export function MarkdownButtonCode() {
  return (
    <MarkdownButton>
      <CodeXml size={16} />
    </MarkdownButton>
  );
}
