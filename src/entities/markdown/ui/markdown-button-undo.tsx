import { Undo2 } from "lucide-react";
import { MarkdownButton } from "./markdown-button";

export function MarkdownButtonUndo() {
  return (
    <MarkdownButton>
      <Undo2 size={16} />
    </MarkdownButton>
  );
}
